<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Read User</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">
    <div class="bg-white p-6 rounded-lg shadow-lg w-1/3">
        <h2 class="text-xl font-bold mb-4 text-center">User Details</h2>
        
        <% if (error) { %>
            <p class="text-red-500 text-center mb-4"><%= error %></p>
        <% } else { %>
            <p><strong>Name:</strong> <%= user.name %></p>
            <p><strong>Email:</strong> <%= user.email %></p>
            <p><strong>Age:</strong> <%= user.age %></p>
            
            <div class="mt-4">
                <button onclick="toggleId()" class="bg-blue-500 text-white px-4 py-2 rounded">Show/Hide ID</button>
                <p id="userId" class="hidden mt-2 text-gray-700"><strong>ID:</strong> <%= user.id %></p>
            </div>
        <% } %>
        
        <a href="/" class="block text-center bg-gray-500 text-white px-4 py-2 rounded mt-4">Back</a>
    </div>
    
    <script>
        function toggleId() {
            let idElement = document.getElementById("userId");
            idElement.classList.toggle("hidden");
        }
    </script>
</body>
</html>
