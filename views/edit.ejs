<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">
    <div class="bg-white p-6 rounded-lg shadow-lg w-1/3">
        <h2 class="text-xl font-bold mb-4 text-center">Edit User</h2>
        
        <form action="/update/<%= user.id %>" method="POST" class="space-y-4">
            <input type="text" name="name" value="<%= user.name %>" required minlength="2" maxlength="50" class="w-full p-2 border rounded">
            <input type="email" name="email" value="<%= user.email %>" required pattern="[^@\s]+@[^@\s]+\.[^@\s]+" class="w-full p-2 border rounded">
            <input type="number" name="age" value="<%= user.age %>" required min="0" max="150" class="w-full p-2 border rounded">
            
            <% if (error) { %>
                <p class="text-red-500 text-sm"><%= error %></p>
            <% } %>
            
            <button type="submit" class="bg-green-500 text-white px-4 py-2 rounded w-full">Update</button>
        </form>
        
        <a href="/" class="block text-center bg-gray-500 text-white px-4 py-2 rounded mt-4">Back</a>
    </div>
</body>
</html>
