<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">

    <div class="bg-white p-6 rounded-lg shadow-lg w-1/3">
        <h2 class="text-xl font-bold mb-4 text-center">CRUD Operations</h2>

        <form action="/upload" method="POST" class="space-y-4">
            <input type="text" name="name" placeholder="Enter Name" required minlength="2" maxlength="50" class="w-full p-2 border rounded">
            <input type="email" name="email" placeholder="Enter Email" required pattern="[^@\s]+@[^@\s]+\.[^@\s]+" class="w-full p-2 border rounded">
            <input type="number" name="age" placeholder="Enter Age" required min="0" max="150" class="w-full p-2 border rounded">

            <% if (error) { %>
                <p class="text-red-500 text-sm"><%= error %></p>
            <% } %>

            <div class="flex justify-between">
                <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Upload</button>
                <button type="button" onclick="clearFields()" class="bg-red-500 text-white px-4 py-2 rounded">Create New</button>
            </div>
        </form>

        <form action="/search" method="GET" class="mt-4">
            <input type="text" name="q" placeholder="Search by Name or Email" class="w-full p-2 border rounded">
            <button type="submit" class="bg-green-500 text-white px-4 py-2 rounded w-full mt-2">Search</button>
        </form>
    </div>

    <script>
        function clearFields() {
            document.querySelectorAll('input').forEach(input => input.value = '');
        }
    </script>

</body>
</html>
